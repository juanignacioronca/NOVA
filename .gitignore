# Secretos — NUNCA commitear (ver CLAUDE.md §5)
.env

# Registros de agentes (los lee la auditoría, no van al repo)
logs/

# Datos locales (calendario, memoria, etc.)
data/

# Fotos/voz para enrolar personas (privado, biométrico) — nunca al repo
personas/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/

# Backups que genera el editor de configuración (modo configuración)
*.yaml.bak

# OS / editor
.DS_Store
.idea/
.vscode/
